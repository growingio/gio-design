import { Subtitle, ArgsTable } from '@storybook/addon-docs/blocks';
import Breadcrumb from './index';

# Breadcrumb 面包屑

<Subtitle>显示当前页面在系统层级结构中的位置，并能向上返回。</Subtitle>

## 使用规范

出现在所有 1+n 级页面的左上角，其中 n 大于等于 1。

## 参数说明

<ArgsTable of={Breadcrumb} />

## 代码演示

### 默认

配置routes。

[Example](/?path=/story/basic-components-breadcrumb--default)