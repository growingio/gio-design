import { Subtitle, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import DateSelector from './index';

# DateSelector 日期选择框

<Subtitle>当用户需要一个日期，可以点击选择框，弹出日期面板进行选择。</Subtitle>

## 代码演示

### 基本样式

最简单的用法，在浮层中可以选择日期。

<Canvas>
  <Story id="components-dateselector--basic" />
</Canvas>

### 禁止选择部分日期

可用 `disabledDate` 禁止选择部分日期.

<Canvas>
  <Story id="components-dateselector--disabled-date" />
</Canvas>

## 参数说明

<ArgsTable of={DateSelector} />
