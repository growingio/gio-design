import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction" />

# GrowingIO Design

GrowingIO Design is a design system in use at GrowingIO to build products that need to share our aesthetics.
It distributes UI React components and static assets for use in building web layouts.
Alongside the React components is a `LESS/CSS` layer that can be used independently on its own.
