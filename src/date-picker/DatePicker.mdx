import { Subtitle, ArgsTable, Story, Canvas } from '@storybook/addon-docs';
import DatePicker from './index';

# DatePicker 日期选择器

<Subtitle>当用户需要一个日期，可以在面板中进行选择。</Subtitle>

## 代码演示

### 基本样式

最简单的用法，在面板中可以选择日期。

<Canvas>
  <Story id="pickers-datepicker--basic" />
</Canvas>

### 禁止选择部分日期

可用 `disabledDate` 禁止选择部分日期.

<Canvas>
  <Story id="pickers-datepicker--disabled-date" />
</Canvas>

## 参数说明

<ArgsTable of={DatePicker} />
